pipeline {
  agent any
  environment {
    WORKINGDIR = "infrastructure/repositories"
    TRIGGER = sh(
      script: "dirname \$(git diff --name-only --diff-filter=AMR HEAD^ ${WORKINGDIR}) 2>/dev/null || echo 'NOTHING'",
      returnStdout: true
    )
  }
  stages {
    stage('Review Terraform Plan') {
      when {
        beforeAgent true
        allOf {
          changeRequest target: 'master'
          expression { TRIGGER != 'NOTHING' }
        }
      }
      steps{
        dir("${WORKINGDIR}/module/test") {
          sh """
          go test -mod vendor
          """
        }
        dir(WORKINGDIR) {
          sh """
          terraform version
          terraform init
          terraform plan
          """
        }
      }
    }
    stage('Apply Terraform') {
      when {
        beforeAgent true
        allOf {
          branch 'master'
          expression { TRIGGER != 'NOTHING' }
        }
      }
      steps{
        dir(WORKINGDIR) {
          sh """
          terraform init
          terraform apply
          """
        }
      }
    }
  }

}